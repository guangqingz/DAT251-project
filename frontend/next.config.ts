import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    allowedDevOrigins: ['10.0.0.6'], // TODO mobile testing remove later, same with in package.json
};

export default nextConfig;
